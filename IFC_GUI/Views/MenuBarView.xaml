<UserControl x:Class="IFC_GUI.Views.MenuBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IFC_GUI.Views"
             
             
             
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="800">
    <Menu>
        <MenuItem Header="_File">
            <MenuItem x:Name="BtnNewFile" Header="_New" InputGestureText="Ctrl+N" Click="BtnNewFile_Click"/>
            <Separator />
            <MenuItem x:Name="BtnOpenFile" Header="_Open" InputGestureText="Ctrl+O" Click="BtnOpenFile_Click"/>
            <Separator />
            <MenuItem x:Name="BtnSaveFile" Header="_Save" InputGestureText="Ctrl+S" Click="BtnSaveFile_Click"/>
            <MenuItem x:Name="BtnSaveAsFile" Header="Save As"/>
            <Separator />
            <MenuItem x:Name="BtnExit" Header="_Exit" InputGestureText="Ctrl+E" Click="BtnExitFile_Click"/>
        </MenuItem>
    </Menu>
</UserControl>
